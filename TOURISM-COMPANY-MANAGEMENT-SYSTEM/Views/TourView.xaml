<UserControl x:Class="TOURISM_COMPANY_MANAGEMENT_SYSTEM.Views.TourView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1100"
             Background="#F5F7FA">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Search Region -->
            <RowDefinition Height="*"/>    <!-- DataGrid & Form Region -->
        </Grid.RowDefinitions>

        <!-- ================= SEARCH REGION ================= -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15" BorderBrush="#E0E6ED" BorderThickness="1">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="✈ TOUR TEMPLATES MANAGEMENT" FontSize="22" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>
                    <Button x:Name="BtnManageDestinations" Grid.Column="1" Content="📍 Manage Destinations" Width="180" Height="32" Background="#8E44AD" Foreground="White" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand" Click="BtnManageDestinations_Click"/>
                </Grid>
                
                <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <StackPanel Margin="0,0,15,10">
                        <TextBlock Text="Search Tour Name" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtSearchName" Width="180" Height="30" Padding="5"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,15,10">
                        <TextBlock Text="Filter Destination" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <ComboBox x:Name="CboSearchDestination" Width="180" Height="30" Padding="5"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,15,10">
                        <TextBlock Text="Min Price" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtSearchMinPrice" Width="120" Height="30" Padding="5"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,15,10">
                        <TextBlock Text="Max Price" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtSearchMaxPrice" Width="120" Height="30" Padding="5"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,0,0,10">
                        <Button x:Name="BtnSearch" Content="Search" Width="90" Height="30" Background="#3498DB" Foreground="White" BorderThickness="0" Margin="0,0,10,0" Cursor="Hand" Click="BtnSearch_Click"/>
                        <Button x:Name="BtnClearSearch" Content="Reset All" Width="90" Height="30" Background="#95A5A6" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnClear_Click"/>
                    </StackPanel>
                </WrapPanel>
            </StackPanel>
        </Border>

        <!-- ================= MAIN CONTENT ================= -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- DataGrid -->
            <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="10" Margin="0,0,15,0" BorderBrush="#E0E6ED" BorderThickness="1">
                <DataGrid x:Name="DataGridTours" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" SelectionMode="Single" Background="Transparent" RowHeaderWidth="0" BorderThickness="0" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#ECF0F1" SelectionChanged="DataGridTours_SelectionChanged">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F8F9FA"/>
                            <Setter Property="Foreground" Value="#2C3E50"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="10"/>
                        </Style>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Padding" Value="0,5"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Code" Binding="{Binding TourCode}" Width="*" MinWidth="70"/>
                        <DataGridTextColumn Header="Tour Name" Binding="{Binding TourName}" Width="3*"/>
                        <DataGridTextColumn Header="Destination" Binding="{Binding DestinationName}" Width="2*" MinWidth="120"/>
                        <DataGridTextColumn Header="Price" Binding="{Binding PricePerPerson}" Width="*" MinWidth="100"/>
                        <DataGridTextColumn Header="Days" Binding="{Binding DurationDays}" Width="*" MinWidth="80"/>
                        
                        <DataGridTextColumn Header="Capacity" Binding="{Binding MaxCapacity}" Width="*" MinWidth="80"/>

                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Input Form -->
            <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="15" BorderBrush="#E0E6ED" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Tour Template Details" FontSize="18" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,15"/>

                        <TextBox x:Name="TxtTourId" Visibility="Collapsed"/> 

                        <TextBlock Text="Tour Code (*)" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtTourCode" Height="30" Padding="5" Margin="0,0,0,10" Background="#F1F2F6"/>

                        <TextBlock Text="Tour Name (*)" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtTourName" Height="30" Padding="5" Margin="0,0,0,10"/>

                        <TextBlock Text="Destination" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <ComboBox x:Name="CboDestination" Height="30" Padding="5" Margin="0,0,0,10"/>

                        <TextBlock Text="Price Per Person (*)" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtPrice" Height="30" Padding="5" Margin="0,0,0,10"/>

                        <TextBlock Text="Duration (Days)" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtDuration" Height="30" Padding="5" Margin="0,0,0,10"/>

                        <TextBlock Text="Max Capacity" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtMaxCapacity" Height="30" Padding="5" Margin="0,0,0,25"/>

                        <Button x:Name="BtnManageSchedules" Content="View/Manage Schedules" Height="35" Background="#8E44AD" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" IsEnabled="False" Click="BtnManageSchedules_Click" Margin="0,0,0,15"/>

                        <!-- Action Buttons -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Button x:Name="BtnAdd" Grid.Column="0" Content="Add New" Height="38" Background="#2ECC71" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="BtnAdd_Click"/>
                            <Button x:Name="BtnUpdate" Grid.Column="2" Content="Update" Height="38" Background="#F1C40F" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" IsEnabled="False" Click="BtnUpdate_Click"/>
                        </Grid>

                        <Grid Margin="0,5,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Button x:Name="BtnDelete" Grid.Column="0" Content="Delete" Height="35" Background="#E74C3C" Foreground="White" BorderThickness="0" Cursor="Hand" IsEnabled="False" Click="BtnDelete_Click"/>
                            <Button x:Name="BtnClear" Grid.Column="2" Content="Clear Form" Height="35" Background="#BDC3C7" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnClear_Click"/>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
