<UserControl x:Class="TOURISM_COMPANY_MANAGEMENT_SYSTEM.Views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="UserControl_Loaded">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Customer Management" FontSize="22"
                   FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,8"/>

        <!-- Read-only notice for Receptionist / Guide / Driver -->
        <Border Grid.Row="1" Background="#FFF3CD" BorderBrush="#F59E0B"
                BorderThickness="1" CornerRadius="4" Padding="10,6"
                Margin="0,0,0,10">
            <TextBlock x:Name="TxtReadOnlyNotice"
                       Text="👁 You have read-only access to this section."
                       Foreground="#856404" FontSize="12"
                       Visibility="Collapsed"/>
        </Border>

        <!-- Toolbar -->
        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="TxtSearch" Grid.Column="0"
                     Height="34" Padding="10,6" FontSize="13"
                     BorderBrush="#BDC3C7" VerticalContentAlignment="Center"
                     TextChanged="TxtSearch_TextChanged"/>

            <Button x:Name="BtnAdd" Grid.Column="1" Content="+ Add"
                    Width="80" Height="34" Margin="8,0,0,0"
                    Background="#27AE60" Foreground="White" FontWeight="Bold"
                    BorderThickness="0" Cursor="Hand" Click="BtnAdd_Click"/>

            <Button x:Name="BtnEdit" Grid.Column="2" Content="Edit"
                    Width="80" Height="34" Margin="6,0,0,0"
                    Background="#2980B9" Foreground="White" FontWeight="Bold"
                    BorderThickness="0" Cursor="Hand" Click="BtnEdit_Click"/>

            <Button x:Name="BtnDelete" Grid.Column="3" Content="Delete"
                    Width="90" Height="34" Margin="6,0,0,0"
                    Background="#E74C3C" Foreground="White" FontWeight="Bold"
                    BorderThickness="0" Cursor="Hand" Click="BtnDelete_Click"/>
        </Grid>

        <DataGrid x:Name="DgCustomers" Grid.Row="3"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" AlternatingRowBackground="#F9F9F9"
                  BorderBrush="#E0E0E0" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"        Binding="{Binding CustomerId}" Width="50"/>
                <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}"   Width="*"/>
                <DataGridTextColumn Header="Phone"     Binding="{Binding Phone}"      Width="130"/>
                <DataGridTextColumn Header="Email"     Binding="{Binding Email}"      Width="180"/>
                <DataGridTextColumn Header="DOB"       Binding="{Binding DateOfBirth, StringFormat=\{0:yyyy-MM-dd\}}" Width="110"/>
                <DataGridTextColumn Header="Address"   Binding="{Binding Address}"    Width="180"/>
                <DataGridTextColumn Header="Passport"  Binding="{Binding PassportNo}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>