<UserControl x:Class="TOURISM_COMPANY_MANAGEMENT_SYSTEM.Views.DestinationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="#F5F7FA">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header & Search -->
            <RowDefinition Height="*"/>    <!-- Content -->
        </Grid.RowDefinitions>

        <!-- ================= HEADER & SEARCH ================= -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15" BorderBrush="#E0E6ED" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="📍 DESTINATION MANAGEMENT" FontSize="22" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>
                
                <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <StackPanel Margin="0,0,15,10">
                        <TextBlock Text="Search Name" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtSearchName" Width="200" Height="30" Padding="5"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,15,10">
                        <TextBlock Text="Filter Country" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtSearchCountry" Width="180" Height="30" Padding="5"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,0,0,10">
                        <Button x:Name="BtnSearch" Content="Search" Width="90" Height="30" Background="#3498DB" Foreground="White" BorderThickness="0" Margin="0,0,10,0" Cursor="Hand" Click="BtnSearch_Click"/>
                        <Button x:Name="BtnClearSearch" Content="Reset" Width="90" Height="30" Background="#95A5A6" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnClearSearch_Click"/>
                    </StackPanel>
                </WrapPanel>
            </StackPanel>
        </Border>

        <!-- ================= MAIN CONTENT ================= -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- DataGrid -->
            <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="10" Margin="0,0,15,0" BorderBrush="#E0E6ED" BorderThickness="1">
                <DataGrid x:Name="DataGridDestinations" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" SelectionMode="Single" Background="Transparent" RowHeaderWidth="0" BorderThickness="0" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#ECF0F1" SelectionChanged="DataGridDestinations_SelectionChanged">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F8F9FA"/>
                            <Setter Property="Foreground" Value="#2C3E50"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="10"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="1.5*"/>
                        <DataGridTextColumn Header="Country" Binding="{Binding Country}" Width="*"/>
                        <DataGridTextColumn Header="Region" Binding="{Binding Region}" Width="*"/>
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Input Form -->
            <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="15" BorderBrush="#E0E6ED" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Destination Details" FontSize="18" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,15"/>

                        <TextBox x:Name="TxtDestId" Visibility="Collapsed"/> 

                        <TextBlock Text="Destination Name (*)" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtName" Height="30" Padding="5" Margin="0,0,0,10"/>

                        <TextBlock Text="Country (*)" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtCountry" Height="30" Padding="5" Margin="0,0,0,10"/>

                        <TextBlock Text="Region" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtRegion" Height="30" Padding="5" Margin="0,0,0,10"/>

                        <TextBlock Text="Description" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtDescription" Height="80" Padding="5" Margin="0,0,0,20" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top"/>

                        <!-- Action Buttons -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Button x:Name="BtnAdd" Grid.Column="0" Content="Add New" Height="38" Background="#2ECC71" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="BtnAdd_Click"/>
                            <Button x:Name="BtnUpdate" Grid.Column="2" Content="Update" Height="38" Background="#F1C40F" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" IsEnabled="False" Click="BtnUpdate_Click"/>
                        </Grid>

                        <Grid Margin="0,5,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Button x:Name="BtnDelete" Grid.Column="0" Content="Delete" Height="35" Background="#E74C3C" Foreground="White" BorderThickness="0" Cursor="Hand" IsEnabled="False" Click="BtnDelete_Click"/>
                            <Button x:Name="BtnClear" Grid.Column="2" Content="Clear Form" Height="35" Background="#BDC3C7" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnClear_Click"/>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
