<UserControl x:Class="TOURISM_COMPANY_MANAGEMENT_SYSTEM.Views.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:oxy="http://oxyplot.org/wpf"
             mc:Ignorable="d"
             d:DesignHeight="750"
             d:DesignWidth="1100"
             Background="#F5F7FA">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,10" BorderBrush="#E0E6ED" BorderThickness="1">
            <WrapPanel VerticalAlignment="Center">
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Report Type" Margin="0,0,0,4" Foreground="#7F8C8D"/>
                    <ComboBox x:Name="CboReportType" Width="150" Height="30" SelectionChanged="CboReportType_SelectionChanged"/>
                </StackPanel>
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="View Mode" Margin="0,0,0,4" Foreground="#7F8C8D"/>
                    <ComboBox x:Name="CboViewMode" Width="160" Height="30"/>
                </StackPanel>
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="From Date" Margin="0,0,0,4" Foreground="#7F8C8D"/>
                    <DatePicker x:Name="DpFrom" Width="130" Height="30"/>
                </StackPanel>
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="To Date" Margin="0,0,0,4" Foreground="#7F8C8D"/>
                    <DatePicker x:Name="DpTo" Width="130" Height="30"/>
                </StackPanel>
                <Button x:Name="BtnLoad" Content="Load Data" Width="110" Height="30" Margin="0,20,10,0" Background="#3498DB" Foreground="White" BorderThickness="0" Click="BtnLoad_Click"/>
                <Button x:Name="BtnExport" Content="Export Excel" Width="110" Height="30" Margin="0,20,0,0" Background="#2ECC71" Foreground="White" BorderThickness="0" Click="BtnExport_Click"/>
            </WrapPanel>
        </Border>

        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,10">
            <Border Background="White" CornerRadius="8" Padding="12" Margin="0,0,10,0" BorderBrush="#E0E6ED" BorderThickness="1">
                <StackPanel>
                    <TextBlock x:Name="TxtCard1Title" Text="Card 1" Foreground="#7F8C8D"/>
                    <TextBlock x:Name="TxtCard1Value" Text="-" FontSize="22" FontWeight="Bold" Foreground="#2C3E50"/>
                </StackPanel>
            </Border>
            <Border Background="White" CornerRadius="8" Padding="12" Margin="0,0,10,0" BorderBrush="#E0E6ED" BorderThickness="1">
                <StackPanel>
                    <TextBlock x:Name="TxtCard2Title" Text="Card 2" Foreground="#7F8C8D"/>
                    <TextBlock x:Name="TxtCard2Value" Text="-" FontSize="22" FontWeight="Bold" Foreground="#2C3E50"/>
                </StackPanel>
            </Border>
            <Border Background="White" CornerRadius="8" Padding="12" BorderBrush="#E0E6ED" BorderThickness="1">
                <StackPanel>
                    <TextBlock x:Name="TxtCard3Title" Text="Date Range" Foreground="#7F8C8D"/>
                    <TextBlock x:Name="TxtCard3Value" Text="-" FontSize="16" FontWeight="SemiBold" Foreground="#2C3E50"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="10" Margin="0,0,10,0" BorderBrush="#E0E6ED" BorderThickness="1">
                <Grid>
                    <TextBlock x:Name="TxtPrimaryChartTitle" Text="Primary Chart" FontWeight="SemiBold" Margin="5"/>
                    <oxy:PlotView x:Name="PrimaryPlot" Margin="0,30,0,0"/>
                </Grid>
            </Border>
            <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="10" BorderBrush="#E0E6ED" BorderThickness="1">
                <Grid>
                    <TextBlock x:Name="TxtSecondaryChartTitle" Text="Secondary Chart" FontWeight="SemiBold" Margin="5"/>
                    <oxy:PlotView x:Name="SecondaryPlot" Margin="0,30,0,0"/>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="3" Background="White" CornerRadius="8" Padding="10" BorderBrush="#E0E6ED" BorderThickness="1">
            <DataGrid x:Name="DataGridStatistics"
                      AutoGenerateColumns="True"
                      IsReadOnly="True"
                      RowHeaderWidth="0"
                      BorderThickness="0"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#ECF0F1"/>
        </Border>
    </Grid>
</UserControl>
