<UserControl x:Class="TOURISM_COMPANY_MANAGEMENT_SYSTEM.Views.PaymentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             Background="#F5F7FA">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Header & Search -->
        <Border Grid.Row="0" Grid.Column="0" Background="White" CornerRadius="8" Padding="15" Margin="0,0,15,15" BorderBrush="#E0E6ED" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="💳 PAYMENT MANAGEMENT" FontSize="22" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>
                <WrapPanel>
                    <StackPanel Margin="0,0,15,10">
                        <TextBlock Text="Search (Booking Code / Customer / Ref)"/>
                        <TextBox x:Name="TxtSearch" Width="300" Height="30" VerticalContentAlignment="Center"/>
                    </StackPanel>
                    <Button x:Name="BtnSearch" Content="Search" Width="90" Height="30" Background="#3498DB" Foreground="White" Margin="0,15,10,10" Click="BtnSearch_Click"/>
                    <Button x:Name="BtnReset" Content="Reset" Width="90" Height="30" Background="#95A5A6" Foreground="White" Margin="0,15,10,10" Click="BtnReset_Click"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <!-- Payment List -->
        <Border Grid.Row="1" Grid.Column="0" Background="White" CornerRadius="8" Padding="10" Margin="0,0,15,0" BorderBrush="#E0E6ED" BorderThickness="1">
            <DataGrid x:Name="dgPayments" AutoGenerateColumns="False" SelectionMode="Single" IsReadOnly="True" SelectionChanged="dgPayments_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Booking" Binding="{Binding Booking.BookingCode}" Width="110"/>
                    <DataGridTextColumn Header="Customer" Binding="{Binding Booking.Customer.FullName}" Width="*"/>
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat={}{0:N0} VNĐ}" Width="120"/>
                    <DataGridTextColumn Header="Method" Binding="{Binding PaymentMethod}" Width="100"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90"/>
                    <DataGridTextColumn Header="Ref" Binding="{Binding TransactionRef}" Width="100"/>
                    <DataGridTextColumn Header="Paid At" Binding="{Binding PaidAt, StringFormat={}{0:dd/MM HH:mm}}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Payment Form -->
        <Border Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" Background="White" CornerRadius="8" Padding="15" BorderBrush="#E0E6ED" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Process Payment" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                    
                    <TextBox x:Name="txtId" Visibility="Collapsed"/>

                    <TextBlock Text="Select Booking" Margin="0,10,0,2"/>
                    <ComboBox x:Name="CbBooking" DisplayMemberPath="BookingCode" SelectedValuePath="BookingId" Height="30" Margin="0,0,0,5" SelectionChanged="CbBooking_SelectionChanged"/>
                    <TextBlock x:Name="TbBookingInfo" Text="Select a booking to see details" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,10"/>

                    <TextBlock Text="Amount (VNĐ)" Margin="0,5,0,2"/>
                    <TextBox x:Name="TxtAmount" Height="30" Margin="0,0,0,10"/>

                    <TextBlock Text="Payment Method" Margin="0,5,0,2"/>
                    <ComboBox x:Name="CbMethod" Height="30" Margin="0,0,0,10">
                        <ComboBoxItem Content="Cash" IsSelected="True"/>
                        <ComboBoxItem Content="BankTransfer"/>
                        <ComboBoxItem Content="Card"/>
                        <ComboBoxItem Content="EWallet"/>
                    </ComboBox>

                    <TextBlock Text="Notes" Margin="0,5,0,2"/>
                    <TextBox x:Name="TxtNotes" Height="60" AcceptsReturn="True" Margin="0,0,0,20"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button x:Name="BtnCreate" Grid.Column="0" Content="Register Payment" Height="40" Background="#2ECC71" Foreground="White" FontWeight="Bold" Click="BtnCreate_Click"/>
                        <Button x:Name="BtnProcess" Grid.Column="2" Content="💸 PAY NOW (FAKE)" Height="40" Background="#E67E22" Foreground="White" FontWeight="Bold" IsEnabled="False" Click="BtnProcess_Click"/>
                    </Grid>
                    
                    <Button x:Name="BtnClear" Content="Clear Form" Height="35" Background="#BDC3C7" Margin="0,15,0,0" Click="BtnClear_Click"/>

                    <Border Background="#EBF5FB" BorderBrush="#AED6F1" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,30,0,0">
                        <StackPanel>
                            <TextBlock Text="Security Tip" FontWeight="Bold" Foreground="#2E86C1" Margin="0,0,0,5"/>
                            <TextBlock Text="Always verify the transaction reference on your banking app before marking as Paid." FontSize="12" Foreground="#5D6D7E" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
